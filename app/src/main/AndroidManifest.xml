<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- No BOOT_COMPLETED needed: the system automatically re-binds the
         WallpaperService after reboot if it was the selected wallpaper. -->

    <application
        android:name=".LiveWallpaperApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LiveWallpaper">

        <!-- Configuration Activity: launched from the wallpaper picker
             (via wallpaper.xml settingsActivity) and from the launcher. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.LiveWallpaper">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Native live wallpaper entry point. This is what makes the app
             appear in Android / Samsung One UI's wallpaper picker. -->
        <service
            android:name=".wallpaper.LiveWallpaperService"
            android:exported="true"
            android:directBootAware="true"
            android:enabled="true"
            android:label="@string/wallpaper_name"
            android:description="@string/wallpaper_description"
            android:permission="android.permission.BIND_WALLPAPER">
            <intent-filter>
                <action android:name="android.service.wallpaper.WallpaperService" />
            </intent-filter>
            <meta-data
                android:name="android.service.wallpaper"
                android:resource="@xml/wallpaper" />
        </service>
    </application>
</manifest>
